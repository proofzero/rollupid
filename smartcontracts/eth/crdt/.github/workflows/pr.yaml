---
name: test and build
on:
  pull_request:
    branches:
      - main

jobs:

  # crdt
  # ----------------------------------------------------------------------------
  build-test-crdt:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v2
      - name: Cache node modules
        id: cache-npm
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          # npm cache files are stored in `~/.npm` on Linux/macOS
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('/crdt/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      - uses: actions/setup-node@v3
        with:
          node-version: lts/gallium
          cache: 'npm'
          cache-dependency-path: ./package-lock.json
      - run: npm install
        # working-directory: crdt
      - name: Build and Test
        run: npm test
        # working-directory: crdt
        env:
          ETHERSCAN_API_KEY: ${{ secrets.ETHERSCAN_API_KEY }}
          GOERLI_OWNER_PRIVATE: ${{ secrets.GOERLI_OWNER_PRIVATE }}
          GOERLI_OWNER_ADDRESS: ${{ secrets.GOERLI_OWNER_ADDRESS }}
          GOERLI_ALCHEMY_URL: ${{ secrets.GOERLI_ALCHEMY_URL }}
          MUMBAI_ALCHEMY_URL: ${{ secrets.MUMBAI_ALCHEMY_URL }}
          POLYGON_ALCHEMY_URL: ${{ secrets.POLYGON_ALCHEMY_URL }}
          ETHEREUM_ALCHEMY_URL: ${{ secrets.ETHEREUM_ALCHEMY_URL }}