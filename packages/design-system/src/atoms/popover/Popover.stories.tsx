import React from "react"
import {
    PopoverExample
} from "./Popover"

export default {
    title: 'Atoms/Popover/Example',
    component: PopoverExample,
}

const Template = (args) => (
    <PopoverExample />
)

export const Default = Template.bind({})
