{
  "name": "@kubelt/components",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "bit install && bit build --skip-tests && bit link && bit snap --skip-tests && bit export",
    "test": "bit test",
    "start": "bit start"
  },
  "devDependencies": {
    "@teambit/bvm": "^0.2.3",
    "yarn-run-all": "3.1.1"
  },
  "packageManager": "yarn@3.2.4"
}
