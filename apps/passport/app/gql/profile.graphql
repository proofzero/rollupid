query getProfile {
  profile {
    displayName
    pfp {
      ... on StandardPFP {
        image
      }
      ... on NFTPFP {
        image
        isToken
      }
    }
    ... on ThreeIDProfile {
      defaultAddress
      cover
      location
      job
      bio
      website
    }
  }
}

mutation updateProfile($profile: ThreeIDProfileInput) {
  updateThreeIDProfile(profile: $profile)
}
