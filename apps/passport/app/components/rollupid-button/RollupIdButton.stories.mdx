import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { RollupIdButton } from './RollupIdButton'

<Meta title="Components/RollupIdButton" component={RollupIdButton} />

export const Template = (args) => <RollupIdButton {...args} />

# Authentication

<Canvas>
  <Story
    name="Default"
    args={{
      href: '#',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Custom Text"
    args={{
      text: 'Login To Rollup',
      href: '#',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
