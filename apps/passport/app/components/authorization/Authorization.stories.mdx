import { Canvas, Meta, Story } from '@storybook/addon-docs'
import { Authorization } from './Authorization'

import circleLogo from '../../assets/circle-logo.svg'

<Meta title="Components/Authorization" component={Authorization} />

export const Template = (args) => <Authorization {...args} />

# Authorization

<Canvas>
  <Story
    decorators={[
      (Story) => (
        <div style={{ width: 482, padding: 12, margin: 'auto' }}>
          <Story />
        </div>
      ),
    ]}
    name="Authorization"
    args={{
      appProfile: {
        clientName: 'Rollup Profile',
        published: true,
        icon: circleLogo,
        scopes: [
          'scope://rollup.id/profile.write',
          'scope://rollup.id/connected-accounts.read',
          'scope://rollup.id/blockchain-account/{{ account }}.manage',
        ],
        redirectURI: 'localhost:9797/oauth',
        termsURL: '',
        websiteURL: 'localhost:9797',
        mirrorURL: '',
        discordUser: '',
        mediumUser: '',
        twitterUser: '',
      },
      userProfile: {
        name: 'John Doe',
        pfp: {
          image: 'https://data.rareapepes.com/5400.png',
          isToken: true,
        },
      },
      scopeMeta: {
        'scope://rollup.id/profile.read': {
          name: 'Read Profile',
          description: 'Read your profile information',
          class: 'account',
        },
        'scope://rollup.id/profile.write': {
          name: 'Edit Profile',
          description: 'Edit your profile information',
          class: 'account',
        },
        'scope://rollup.id/connected-accounts.read': {
          name: 'Read Blockchain Accounts',
          description: 'See your viewable connected blockchain accounts',
          class: 'address',
        },
        'scope://rollup.id/blockchain-account/{{ account }}.manage': {
          name: 'Create Dedicated Blockchain Account',
          description:
            'Create and manage a dedicated blockchain account isolated for this application.',
          class: 'address',
        },
      },
      transition: {
        state: 'idle',
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
