import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { ThreeIdButton } from './ThreeIdButton'

<Meta title="Components/ThreeIdButton" component={ThreeIdButton} />

export const Template = (args) => <ThreeIdButton {...args} />

# Authentication

<Canvas>
  <Story
    name="Default"
    args={{
      href: '#',
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Custom Text"
    args={{
      text: 'Login To 3ID',
      href: '#',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
