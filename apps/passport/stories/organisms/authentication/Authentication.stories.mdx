<!-- Authentication.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs'

import { Authentication } from './Authentication'

<Meta title="Organisms/Authentication" component={Authentication} />

# Authentication

<Canvas>
  <Story name="Auth with Wallet">
    <Authentication
      connectCallback={(address) => alert(`connected to ${address}`)}
      errorCallback={(error) => alert(`Failed to connect: ${error}`)}
    />
  </Story>
</Canvas>
