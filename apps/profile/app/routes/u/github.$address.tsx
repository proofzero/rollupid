// 1. fetch edge with $address (github username) alias

// 2. if edge exists (and not hidden) get idref from edge

// 3. check if handle exists and redirecto to /u/$handle

// 4. if no handle redirect to /u/$idref

// 5. if no idref or hidden return 404
