import { LoginsPanel } from './LoginsPanel'

export default {
  title: 'Molecules/Applications/Dashboard/Logins',
  component: LoginsPanel,
}

const Template = () => <LoginsPanel />

export const Default = Template.bind({})
