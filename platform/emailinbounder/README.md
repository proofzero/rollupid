# Email Inbounder Worker

## Overview

This worker processes inbound emails received from the global distributor worker, in an env-specific way

## Setup

### Local Env

1. Copy `.dev.vars.example` to `.dev.vars` and fill in the values.
