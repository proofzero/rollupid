generates:
  src/schema.ts:
    schema:
      - '../../packages/urns/gql/**/*.graphql'
      - '../../packages/types/gql/address.graphql'
      - 'gql/**/*.graphql'
    plugins:
      - typescript-validation-schema
    config:
      schema: zod
      withObjectType: true
      enumsAsTypes: false
      importFrom: '../../packages/types'
