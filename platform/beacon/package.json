{
  "name": "@kubelt/platform.beacon",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "3.18.0",
    "concurrently": "7.5.0",
    "npm-run-all": "4.1.5",
    "typescript": "4.8.4",
    "wrangler": "2.4.4"
  },
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "build": "wrangler publish --dry-run --outdir=dist",
    "dev:wrangler": "wrangler dev --local --persist",
    "dev": "run-p 'dev:*'",
    "start": "echo \"No start for galaxy. Please use dev.\""
  },
  "dependencies": {
    "itty-router": "2.6.6"
  }
}
