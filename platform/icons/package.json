{
  "name": "@kubelt/app.icons",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "3.18.0",
    "typescript": "4.8.4",
    "wrangler": "2.1.13"
  },
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "dev:wrangler": "wrangler dev --local --persist --env dev",
    "dev": "echo \"No dev for redeem. Please use start.\"",
    "start": "run-p 'dev:*'",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "date-fns": "2.29.3",
    "tiny-invariant": "1.3.1"
  }
}
