{
  "name": "@kubelt/platform.icons",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "3.18.0",
    "typescript": "4.8.4",
    "wrangler": "2.1.15"
  },
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "dev:wrangler": "wrangler dev --local --persist",
    "dev": "run-p \"dev:*\"",
    "start": "run-p 'dev:*'",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "date-fns": "2.29.3",
    "tiny-invariant": "1.3.1"
  }
}
